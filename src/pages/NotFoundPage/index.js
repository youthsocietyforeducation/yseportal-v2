import React from 'react';

const propTypes = {};
const defaultProps = {};

const NotFoundPage = () => {
  return <div data-testid="NotFoundPage">PAGE NOT FOUND =/</div>;
};

NotFoundPage.propTypes = propTypes;
NotFoundPage.defaultProps = defaultProps;

export default NotFoundPage;
