import React from 'react';

const propTypes = {};
const defaultProps = {};

const Home = () => {
  return <div data-testid="LogIn">Welcome from Home</div>;
};

Home.propTypes = propTypes;
Home.defaultProps = defaultProps;

export default Home;
